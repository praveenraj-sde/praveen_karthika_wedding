[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"

# /wedding → root (same invitation page)
[[redirects]]
  from = "/wedding"
  to = "/"
  status = 301
